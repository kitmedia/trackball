# Story 1.2: User Authentication & Basic Team Structure

## Status
🟡 **Draft** - Ready for implementation

## Story
**As a** coach,
**I want** to create an account and authenticate securely,
**so that** I can access the video analysis platform with my team's private data protected.

## Acceptance Criteria
1. User registration with email verification and password requirements
2. OAuth 2.0 authentication system with JWT token management
3. Basic user profile management (name, email, role)
4. Team creation and invitation system for multiple users
5. Role-based access control foundation (coach, analyst, viewer roles)
6. Password reset and account recovery functionality
7. Session management with automatic logout and security features
8. GDPR-compliant user data handling and privacy controls

## Tasks / Subtasks

- [ ] **Task 1: Database Schema Implementation** (AC: 1, 2, 3, 4, 5)
  - [ ] Create PostgreSQL tables for users, teams, and team_members using provided schema
  - [ ] Add indexes for email, username, and foreign key relationships
  - [ ] Create Alembic migration scripts for database versioning
  - [ ] Implement SQLAlchemy models in `apps/api/src/models/user.py` and `apps/api/src/models/team.py`
  - [ ] Add test data seed scripts for development environment
  
- [ ] **Task 2: Authentication Backend Implementation** (AC: 1, 2, 6, 7)
  - [ ] Implement JWT token generation and validation in `apps/api/src/core/auth.py`
  - [ ] Create FastAPI authentication endpoints in `apps/api/src/api/auth.py`:
    - POST /auth/register (user registration)
    - POST /auth/login (user login)
    - POST /auth/refresh (token refresh)
    - POST /auth/logout (user logout)
    - POST /auth/reset-password (password reset request)
    - PUT /auth/reset-password/{token} (password reset confirmation)
  - [ ] Implement password hashing using bcrypt
  - [ ] Create authentication middleware for protected routes
  - [ ] Add rate limiting for authentication endpoints (10 requests/minute)
  - [ ] Implement email verification service using AWS SES or similar

- [ ] **Task 3: User and Team Management API** (AC: 3, 4, 5)
  - [ ] Create user management endpoints in `apps/api/src/api/users.py`:
    - GET /users/me (get current user profile)
    - PUT /users/me (update user profile)
    - DELETE /users/me (GDPR-compliant account deletion)
  - [ ] Create team management endpoints in `apps/api/src/api/teams.py`:
    - GET /teams (list user's teams)
    - POST /teams (create new team)
    - GET /teams/{team_id} (get team details)
    - PUT /teams/{team_id} (update team)
    - DELETE /teams/{team_id} (delete team - owner only)
  - [ ] Implement team member management:
    - GET /teams/{team_id}/members (list team members)
    - POST /teams/{team_id}/members (invite team member)
    - PUT /teams/{team_id}/members/{user_id} (update member role)
    - DELETE /teams/{team_id}/members/{user_id} (remove member)
  - [ ] Create service layer in `apps/api/src/services/user_service.py` and `team_service.py`
  - [ ] Implement repository pattern for database access

- [ ] **Task 4: Frontend Authentication Components** (AC: 1, 2, 3, 6)
  - [ ] Create authentication pages in `apps/web/src/pages/auth/`:
    - LoginPage.tsx (email/password login)
    - RegisterPage.tsx (new user registration)
    - ForgotPasswordPage.tsx (password reset request)
    - ResetPasswordPage.tsx (password reset with token)
  - [ ] Implement authentication store using Zustand in `apps/web/src/store/authStore.ts`
  - [ ] Create authentication service in `apps/web/src/services/auth.service.ts`
  - [ ] Implement useAuth hook in `apps/web/src/hooks/useAuth.ts`
  - [ ] Create ProtectedRoute component for route guards
  - [ ] Add authentication context provider to App.tsx

- [ ] **Task 5: Frontend Team Management UI** (AC: 4, 5)
  - [ ] Create team management pages in `apps/web/src/pages/teams/`:
    - TeamListPage.tsx (list user's teams)
    - TeamDetailPage.tsx (team details and members)
    - CreateTeamPage.tsx (new team creation)
  - [ ] Implement team invitation flow with email notifications
  - [ ] Create team member management UI with role assignment
  - [ ] Add team switching functionality to application header
  - [ ] Implement team-scoped data access controls

- [ ] **Task 6: Session Management & Security** (AC: 7, 8)
  - [ ] Implement JWT token storage using httpOnly cookies
  - [ ] Add automatic token refresh on API calls
  - [ ] Create session timeout handling (24-hour access tokens, 30-day refresh tokens)
  - [ ] Implement automatic logout on inactivity
  - [ ] Add GDPR compliance features:
    - Data export functionality
    - Account deletion with data purge
    - Privacy policy acceptance tracking
    - Cookie consent management
  - [ ] Create security headers middleware (CORS, CSP, etc.)

- [ ] **Task 7: Testing Implementation** (AC: All)
  - [ ] Write unit tests for authentication services (pytest)
  - [ ] Create integration tests for all API endpoints
  - [ ] Implement frontend component tests using Vitest + RTL
  - [ ] Add E2E tests for authentication flows using Playwright
  - [ ] Test rate limiting and security features
  - [ ] Verify GDPR compliance functionality

## Dev Notes

### Previous Story Context
Story 1.1 successfully implemented the project foundation with:
- Nx monorepo structure with React frontend and FastAPI backend
- Docker containerization with PostgreSQL and Redis
- Basic middleware setup including CORS and logging
- GitHub Actions CI/CD pipeline

### Database Schema
[Source: architecture/database-schema.md#Users and Authentication]

```sql
-- Users table structure
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    username VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    first_name VARCHAR(100),
    last_name VARCHAR(100),
    role VARCHAR(50) NOT NULL DEFAULT 'analyst', -- admin, coach, analyst
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    last_login TIMESTAMP WITH TIME ZONE
);

-- Teams and team_members tables (see full schema in architecture docs)
```

### API Specifications
[Source: architecture/api-specification.md#Authentication Endpoints]

Authentication endpoints return JWT tokens with the following structure:
```json
{
  "access_token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "refresh_token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "token_type": "bearer",
  "expires_in": 3600,
  "user": {
    "id": "123e4567-e89b-12d3-a456-426614174000",
    "email": "user@example.com",
    "username": "analyst1",
    "role": "analyst"
  }
}
```

### File Locations
[Source: architecture/unified-project-structure.md]

Backend files to create/modify:
- `apps/api/src/models/user.py` - User SQLAlchemy model
- `apps/api/src/models/team.py` - Team and TeamMember models
- `apps/api/src/api/auth.py` - Authentication endpoints
- `apps/api/src/api/users.py` - User management endpoints
- `apps/api/src/api/teams.py` - Team management endpoints
- `apps/api/src/core/auth.py` - Authentication logic
- `apps/api/src/core/security.py` - Security utilities
- `apps/api/src/services/user_service.py` - User business logic
- `apps/api/src/services/team_service.py` - Team business logic
- `apps/api/src/schemas/user.py` - Pydantic schemas
- `apps/api/src/schemas/team.py` - Team schemas

Frontend files to create:
- `apps/web/src/pages/auth/` - Authentication pages
- `apps/web/src/pages/teams/` - Team management pages
- `apps/web/src/store/authStore.ts` - Zustand auth store
- `apps/web/src/services/auth.service.ts` - Auth API client
- `apps/web/src/hooks/useAuth.ts` - Authentication hook
- `apps/web/src/components/auth/ProtectedRoute.tsx` - Route guard

### Technical Constraints
[Source: architecture/tech-stack.md]
- JWT authentication (not AWS Cognito as originally mentioned in PRD)
- FastAPI for backend (version ^0.104.0)
- React 18 with TypeScript for frontend
- PostgreSQL 15+ for database
- Zustand for state management
- Material-UI for UI components

### Security Requirements
[Source: architecture/coding-standards.md#Critical Fullstack Rules]
- Never store JWT tokens in localStorage (use httpOnly cookies)
- All passwords must be hashed using bcrypt
- Implement rate limiting on authentication endpoints
- Use HTTPS only in production
- Validate all inputs using Pydantic schemas

## Testing

### Testing Standards
[Source: architecture/testing-strategy.md]

**Backend Testing (pytest):**
- Test files location: `apps/api/tests/`
- Use FastAPI TestClient for endpoint testing
- Mock external services (email, etc.)
- Minimum 80% code coverage

**Frontend Testing (Vitest + RTL):**
- Test files location: `apps/web/tests/`
- Component tests for all auth pages
- Mock API calls using vi.mock
- Test authentication flows and error states

**E2E Testing (Playwright):**
- Test complete authentication flows
- Verify team creation and member invitation
- Test session timeout and token refresh

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|---------|
| 2025-01-23 | 1.0 | Initial story creation | BMad Master |

## Dev Agent Record
*To be populated during implementation*

### Agent Model Used
*TBD*

### Debug Log References
*TBD*

### Completion Notes List
*TBD*

### File List
*TBD*

## QA Results
*To be populated during QA review*